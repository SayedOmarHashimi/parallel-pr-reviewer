# Template only. Copy to .env (which is gitignored) and fill in locally.
# NEVER commit real values.
DATABASE_URL=postgresql://user:password@localhost:5432/devnotes
JWT_SECRET=replace-me
ADMIN_TOKEN=replace-me
